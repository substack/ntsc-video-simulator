{
  "main": "dist.js",
  "dependencies": {
    "regl": "^2.0.1"
  },
  "scripts": {
    "build": "browserify index.js -o dist.js"
  },
  "browserify": {
    "transform": [
      "glslify"
    ]
  },
  "devDependencies": {
    "glsl-noise": "0.0.0",
    "glslify": "^7.1.1"
  }
}
