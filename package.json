{
  "name": "ntsc-video-simulator",
  "version": "1.0.0",
  "description": "real-time ntsc television simulator",
  "main": "dist.js",
  "scripts": {
    "build": "browserify -s TV -r ./index.js -t glslify -o dist.js"
  },
  "devDependencies": {
    "glsl-noise": "0.0.0",
    "glslify": "^7.1.1",
    "regl": "^2.0.1"
  },
  "license": "bsd"
}
